export enum PublicRoutes {
  LOGIN = "login",
  REGISTER = "register",
}

export enum CommonRoutes {
  HOME = "home",
  ACCOUNT = "account",
}

export enum StudentRoutes {
  STUDENT = "student",
}

export enum TeacherRoutes {
  TEACHER = "teacher",
  DASHBOARD = "dashboard",
}
