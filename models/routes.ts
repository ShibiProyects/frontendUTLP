export enum PublicRoutes {
  LOGIN = "login",
  REGISTER = "register",
  AUTH = "auth",
}

export enum StudentRoutes {
  STUDENT = "student",
  HOME = "home",
  ACCOUNT = "account",
}

export enum TeacherRoutes {
  TEACHER = "teacher",
  DASHBOARD = "dashboard",
  ACCOUNT = "account",
}
